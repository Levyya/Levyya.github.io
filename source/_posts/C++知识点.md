---
title: C++知识点
date: 2022-07-30
tags: C++
categories: 
- 计算机
- C++
---

参考

[C++岗位面试真题宝典](https://www.nowcoder.com/tutorial/10069/ad6b48325414495293d576d648ddcd8d)

[C++课程学习](https://www.nowcoder.com/study/live/504)

[CSDN博客学习](https://blog.csdn.net/weixin_44368437/article/details/117563488?ops_request_misc=%257B%2522request%255Fid%2522%253A%2522166583565716782414920926%2522%252C%2522scm%2522%253A%252220140713.130102334..%2522%257D&request_id=166583565716782414920926&biz_id=0&utm_medium=distribute.pc_search_result.none-task-blog-2~all~top_positive~default-1-117563488-null-null.142^v56^pc_rank_34_1,201^v3^control_2&utm_term=C%2B%2B&spm=1018.2226.3001.4187)



技术栈（学习）

1.熟悉C/C++11编程语言，具有良好的面向对象思想

2.熟悉STL数据结构及算法基础

3.熟悉linux环境编程，熟练掌握Linux下C/C++、多线程、多进程、内存共享、网络通信编程技术

4.熟悉linux下开发工具集使用，如gdb，makefile，git的使用，以及shell脚本，python脚本的编写

5.熟悉qt Gui框架，熟练使用qt weight进行gui开发，了解信号槽机制与元数据机制

6.有能力利用gtest，gmock框架对开发单元进行单元自测

7.熟悉debian包管理，能够完整的从源码到构建deb出包，有完整产品从编码到构建上线应用商店经验



## 书单

语言类：

<C++ Primer>

<Effective C++>

<More Effective C++>

\<Effective STL>

<Effective Modern C++>

<C++ 新经典>

<C++ 新经典：对象模型>

<QtCreator 快速入门>

工程类

《跟我一起写Makefile》

《程序员的自我修养-编译链接与库转载》

《代码整洁之道》

《Google C++ 风格指南》

《Debugging with gdb》

《C/C++ 代码调试的艺术》

《大话设计模式》

《Linux高性能服务器编程》

《TCP/IP 网络编程》

《C++ 新经典：LinuxC++ 通信架构实战》







## C++ 基础

1. C++语言的特点
   1. 面向对象，兼容c
   2. 三大特性：封装、继承、多态
   3. 效率高，比汇编慢10-20%
   4. 安全，const常量、引用、四类cast转换（static_cast, dynamic_cast, const_cast, reinterpret_cast）、智能指针、try-catch
   5. 可复用，引入**模板**，标准模板库**STL**
   6. C++11新特性：nullptr、auto、Lambda、右值引用、智能指针
2. C++代码到可执行二进制文件的过程
   1. 预编译、编译、汇编、链接
3. C++作用域
   1. 全局、局部、类、语句、命名空间、文件作用域



### C++ 内存

堆和栈的区别

1. 堆栈空间分配不同；栈由操作系统自动分配释放，存放函数的参数值，局部变量的值等；堆一般由程序员分配释放；
2. 堆栈缓存方式不同；栈使用一级缓存，调用完立即释放；堆使用二级缓存
3. 堆栈数据结构不同；堆类似数组；栈，先进后出

C++ 内存管理

* 堆
* 栈
* 自由存储区
* 全局、静态存储区
* 常量存储区

一个程序有哪些部分

**从低地址到高地址，一个程序由代码段、数据段、** **BSS** **段组成。**

1. **数据段：**存放程序中已初始化的全局变量和静态变量的一块内存区域。

2. **代码段：**存放程序执行代码的一块内存区域。只读，代码段的头部还会包含一些只读的常数变量。

3. **BSS** 段：存放程序中未初始化的全局变量和静态变量的一块内存区域。

4. 可执行程序在运行时又会多出两个区域：堆区和栈区。

   **堆区：**动态申请内存用。堆从低地址向高地址增长。

   **栈区：**存储局部变量、函数参数值。栈从高地址向低地址增长。是一块连续的空间。

5. 最后还有一个**文件映射区**，位于堆和栈之间。



---

**内存对齐**\*

Union取最长，结构体取总和

## C++ 面向对象

类的构成

包括数据和函数，按访问权限划分，可分为公有、保护、私有

* 一般情况下，类的数据应该声明为私有成员，成员函数声明为公有
* 私有部门处于类的第一部分时，关键字private可以省略
* 如果一个类体中没有访问权限关键字，则数据成员和成员函数默认是私有的

### 友元

友元包括友元函数和友元类，对私有或者保护成员进行访问。



### C++中的构造函数

* 默认构造函数
* 初始化构造函数
* 拷贝构造函数
* 移动构造函数
  * 用于将其他类型的变量，隐式转换为本类对象



### 虚基类

虚基类的作用

如果一个类有多个直接基类，而这些直接基类又有一个共同的基类，则在最低层的派生类中会保留这间接的共同基类数据成员的多份同名成员。在访问这些同名成员时，必须在派生类对象名后增加直接基类名，使其唯一地标识一个成员，以免产生二义性。





### 虚函数和纯虚函数

虚函数的作用是实现了多态，让父类型的指针指向其子类的实例，虚函数是通过一张虚函数表来实现的。（概念类似接口）

纯虚函数是在基类中声明的虚函数，

```c++
// 纯虚函数
virtual void func()=0;
```

### 抽象类

一个类中至少含有一个纯虚函数

* 抽象类只能作为其它类的基类来使用，不能建立抽象类对象
* 不允许从具体类派生出抽象类
* 抽象类不能作为函数的参数类型、函数的返回类型或显示转换的类型
* 可以声明指向抽象类的指针或引用
* 如果派生类没有定义纯虚函数的实现，而派生类中只是继承基类的纯虚函数，则这个派生类仍然是一个抽象类。



`常函数`类的成员函数后面加 const，表明这个函数不会对这个类对象的数据成员（准确地说是非静态数据成员）作任何改变。

虚析构函数

虚基类

### 仿函数

```c++
class Func {
public:
	void operator() (const string& str) const {
		cout << str << endl;
	}
};

void main() {
	Func func;
	func("Hello world!");
}
```



C++ 采用了静态联编和动态联编结合的方式

关于虚函数

* 虽然使用对象名和点运算符的方式也可以调用虚函数，但是这种调用实在编译时进行静态联编，没有充分利用虚函数的特性，只有通过基类指针访问虚函数时才能获得运行时的多态性
* 虚函数必须是其所在类的成员函数，而不能是友元函数，也不能是静态成员函数
* 内联函数不能是虚函数
* 构造函数不能是虚函数
* 析构函数可以是虚函数，而且通常说明为虚函数



## 运算符重载



## 函数模板与类模板



## C++ IO

* ios
* istream
* ifstream
* istrstream
* istrstream
* ostream
* ofstream
* ostrstream
* iostream
* fstream
* strstream



1. put() 
   1. cout.put(单字符、字符型变量、ASCII码)
2. get() 
3. getline() 
   1. cin.getline(字符数组，字符个数，终止标志字符)
   2. cin.getline(字符指针，字符个数，终止标志字符)
4. ignore()

## C++ STL

广义上，分三类：算法、容器、迭代器

具体地，分为六类：算法、容器、迭代器、仿函数、适配器、空间配置器

容器

vector, list, stack, queue, priority_queue, deque, set, map

* 顺序容器
  * vector
  * deque
  * list
* 关联式容器
  * set / multiset
  * map / multimap
* 容器适配器
  * stack
  * queue
  * priority_queue



push_back 和 emplace_back 的区别

push_back()需要先构造临时对象，再将这个对象拷贝到容器的末尾，而emplace_back()则直接在容器的末尾构造对象，这样就省去了拷贝的过程。





静态库和动态库区别

1. 静态库代码装载的速度快，执行速度略比动态库快。
2. 动态库更加节省内存，可执行文件体积比静态库小很多。
3. 静态库是在编译时加载，动态库是在运行时加载。
4. 生成的静态链接库，Windows下以.lib为后缀，Linux下以.a为后缀。生成的动态链接库，Windows下以.dll为后缀，Linux下以.so为后缀。



`零拷贝`计算机操作系统当中，CPU不执行将数据从一个内存区域，拷贝到另外一个内存区域的任务。通过网络传输文件时，这样通常可以节省 CPU 周期和内存带宽。



## C++ 11

lambda 表达式

```c++
[](int x) { return x % 3 == 0; }
```

[val] 按值访问变量

[&val]  按引用访问变量

[&]  按引用访问所有动态变量

[=]  按值访问所有动态变量



## C++ 面经

1. 静态局部变量，全局变量，局部变量的特点

C++作用域：

全局、局部、类、语句、命名空间、文件作用域

* 静态全局变量
  * 全局作用域+文件作用域，无法在其他文件中使用
* 全局变量
  * 全局作用域，可以通过extern作用于其他非定义的源文件
* 局部变量
  * 局部作用域
* 静态局部变量
  * 局部作用域，只被初始化一次

局部变量在栈上分配，其它都在静态存储区



2. const 和 define 的区别

* const
  * 用于定义常量，生效于编译阶段，存储在内存中，需要额外空间，常量带有类型
* define
  * 用于定义宏，生效于预处理阶段，不会存放在内存中，常量不带类型，不利于类型检查



3. C++ 代码到可执行二进制文件的过程
   1. 预编译（预处理）
      1. 删除#define，展开所有的宏定义
      2. 处理所有的条件预编译指令，#if，#ifdef，#ifndef
      3. 处理#include预编译指令，讲被包含的文件插入到该预编译指令的位置
      4. 过滤所有的注释
      5. 添加行号和文件名标识
   2. 编译
      1. 词法分析：代码分词
      2. 语法分析：产生语法树
      3. 语义分析：判断表达式是否有意义
      4. 代码优化
      5. 目标代码生成：生成汇编代码
      6. 目标代码优化
   3. 汇编
      1. 将汇编代码转变成机器码
   4. 链接
      1. 将不同的源文件产生的目标文件进行链接，从而形成一个可执行的程序
      2. 静态链接，.lib，.a，链接时就添加到可执行文件中
      3. 动态链接，.dll，.so，执行时查找



4. 野指针
   * 概念：指向的位置是不可知的
   * 产生原因：
     * 释放内存后不及时置空
     * 未初始化
   * 避免
     * 初始化置NULL
     * 申请内存后判空
     * 指针释放后置NULL
     * 使用智能指针



5. public, protected, private
   1. public：可以被任意实体访问
   2. protected：只允许子类及派生类的成员函数访问
   3. private：只允许本类的成员函数访问





装饰器





RAII (Resource Acquisition Is Initialization) 资源获取就是初始化

RTTI (Runtime Type Identification) 运行阶段类型识别

孤儿进程、僵尸进程、守护进程

**socket通信流程**

单例模式

类型转换

Linux 伙伴系统

四级页表：页全局目录、页上级目录、页中级目录、页表

智能指针：unique_ptr, shared_ptr, weak_ptr





## Visual Studio 使用

快捷键

* Ctrl + K + D  format代码
* Ctrl + K + C  注释代码
* Ctrl + K + U  取消注释
* Ctrl + F5  运行（不调试）

## 项目学习--Drogon

* Https: OpenSSL
* 

